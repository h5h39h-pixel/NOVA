# Trained model usability check (OUT-2)

_Generated by `scripts/nova_eval.py` on 2026-06-30 08:26 Arabian Standard Time — model **nova-local**._

Verifies the *output* of the LoRA training pipeline (`docs/training-pipeline.md`) is a usable model: identity, domain knowledge, and bilingual response. The fine-tune *quality* itself is owner-run on the external training box.

**Result: 4/4 (100%) checks passed.**

| Check | Pass | Reply (truncated) |
|---|---|---|
| identifies as Nova | ✅ | I'm Nova, your bilingual (Arabic/English) AI running locally on the user's Windows 11 PC with an RTX 5090; I manage thei |
| knows the RTX 5090 | ✅ | It's an NVIDIA RTX 5090 with 32GB VRAM. |
| knows its domain | ✅ | It's a web UI for managing your local AI models, tools, and tasks (like an Ollama control center). |
| responds in Arabic (bilingual) | ✅ | أنا نووا، مساعدك الذكي المحلي على PC الخاص بك (Windows 11 مع RTX 5090). أنا هنا لمساعدتك في أي شيء تحتاجه من توليد النص  |

Re-run: `python scripts/nova_eval.py --write-doc`.
