# Agent success baseline (OUT-1)

_Generated by `scripts/agent_eval.py` on 2026-06-30 09:30 Arabian Standard Time — model **qwen2.5:14b**._

Real end-to-end runs of the agent loop against fixed, verifiable goals (safe tools only). This is outcome verification, not a unit test — numbers are model- and run-dependent.

**Result: 9/9 (100%) goals achieved.**

| Goal | Result | Steps | Time | Final (truncated) |
|---|---|---|---|---|
| write_file | ✅ | 1 | 1.0s | NOVA-OK-7731 |
| arithmetic | ✅ | 1 | 1.1s | 391 |
| username | ✅ | 4 | 3.4s | Reached the step limit. Partial progress shown above. |
| read_file | ✅ | 1 | 1.2s | The token contained in eval_secret.txt is TOKEN-4F9A2. |
| multi_step | ✅ | 2 | 2.3s | The number in the file eval5.txt is 42. |
| sum_three | ✅ | 1 | 1.3s | The total is 1368. |
| date_year | ✅ | 1 | 1.2s | The current year according to the retrieved date is 2026. |
| two_files | ✅ | 2 | 2.0s | Created alpha.txt containing A and beta.txt containing B. |
| write_read_reason | ✅ | 2 | 2.2s | The word 'BANANA' has 6 letters. |

> First baseline (2026-06-30) scored **2/5** and exposed a real bug: relative `write_file` paths prefixed with `agent-output/` nested a doubled directory, and `read_file` resolved relative paths against the process CWD instead of the output folder — so the agent could not read back files it wrote. Fixed in M54 (`_strip_output_prefix` + `resolve_read_path`); the battery then scored 5/5.

Re-run: `python scripts/agent_eval.py --model <name> --write-doc`.
